{% extends 'base.html.twig' %}

{% block title %}All articles{% endblock %}

{% block body %}
    <button><a href="{{ path('blog_show_category', { 'categoryName': 'javascript'}) }} ">JavaScript</a></button>
    <button><a href="{{ path('blog_show_category', { 'categoryName': 'java'}) }} ">Java</a></button>
    <button><a href="{{ path('blog_show_category', { 'categoryName': 'c'}) }} ">C</a></button>
    </br>
    <h1>All articles from article s table : </h1>
    {% for article in articles %}
        <div>
            <h2>{{ loop.index }} / {{ article.title }} - Category : {{ article.category.name }}</h2>
            <p>{{ article.content }}</p>
            <a href="{{ path('blog_show', { 'slug': article.title|replace({" ":"-"})|lower  }) }}">En savoir plus</a>
        </div>
    {% else %}
        Aucun article trouvé.
    {% endfor %}
    <a href="{{ path('app_index') }}">
      Back to homepage.
    </a>
{% endblock %}
